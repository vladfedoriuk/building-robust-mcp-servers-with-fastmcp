{
  "name": "presentation",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "slidev build",
    "dev": "slidev --open",
    "export": "slidev export",
    "deploy": "slidev build && wrangler pages deploy dist --project-name=building-robust-mcp-slides",
    "deploy:no-notes": "slidev build --without-notes && wrangler pages deploy dist --project-name=building-robust-mcp-slides"
  },
  "dependencies": {
    "@slidev/cli": "^52.15.0",
    "@slidev/theme-default": "latest",
    "@slidev/theme-seriph": "latest",
    "slidev-theme-geist": "^0.8.1",
    "vue": "^3.5.33"
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  }
}